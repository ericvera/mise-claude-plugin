{
  "name": "workflow-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "prettier": {
    "semi": false
  },
  "devDependencies": {
    "@types/node": "^24",
    "prettier": "^3",
    "typescript": "^5"
  }
}
