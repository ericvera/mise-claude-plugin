# Close

`git rm -r -q .mise` and commit `mise: close`. Then, unless the owner asked for something else, `git push` and open a pull request summarizing the work. A failure there → report the branch and the action you tried.
